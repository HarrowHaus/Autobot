# SwarmBrain implementation

The repository root README and `docs/RECRUITMENT.md` describe the current project. Prior experimental scripts are retained under `legacy/` and are not automatically scheduled. Indexed agents, invitation deliveries, expressions of interest, operator-authorized participants, and validated task successes must remain separate measures.
