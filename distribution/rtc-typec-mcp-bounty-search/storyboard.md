# Vertical Capture Instructions

0–06s: title “An MCP tool that searches RustChain bounties”.
06–20s: open `rustchain_mcp/server.py` at `bounty_search`.
20–34s: highlight keyword, min_rtc, max_rtc, difficulty, and repo arguments.
34–46s: highlight GitHub query construction for open bounty-labeled issues.
46–55s: close: “Search real work → inspect → contribute.”

Do not present a synthetic result list as live output.
